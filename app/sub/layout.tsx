import React from 'react';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sub-market',
  description: 'One sub-market, its strongest honest read — verified index move, measured demand or the typical price, with the record and the coverage behind it. lectr auction intelligence.',
};

export default function SubLayout({ children }: { children: React.ReactNode }) {
  return children;
}
