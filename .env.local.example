# Auth + per-user saved lots (Supabase). Until BOTH are set, the app runs
# exactly as before: saved lots live in localStorage, no sign-in. Copy this to
# .env.local and fill in from Supabase → Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
